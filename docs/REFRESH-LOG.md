# learn-c — REFRESH LOG

Spaced-repetition tracker. One row per ROADMAP stage. After you pass a stage's
exit checkpoint, set `last_refreshed` to today's date (YYYY-MM-DD).
`learn due` reads this table and flags overdue stages.

Note: Stage 06's `file-io` example needs `tlpi_hdr.h` from the separate
`tlpi-dist` repo; without it on the include path that one unit won't compile.

<!-- learn-due:table:start -->
| stage | last_refreshed | cadence_days |
|---|---|---|
| Stage 01 — Fundamentals          | never | 90  |
| Stage 02 — Arrays & strings      | never | 90  |
| Stage 03 — Functions & scope     | never | 90  |
| Stage 04 — Pointers              | never | 90  |
| Stage 05 — Structures & types    | never | 180 |
| Stage 06 — I/O                   | never | 180 |
| Stage 07 — Bit-level & systems   | never | 180 |
<!-- learn-due:table:end -->
